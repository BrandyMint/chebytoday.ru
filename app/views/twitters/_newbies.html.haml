
#newbies
  - if TwitUser.new_listed.count>0
    %h2
      Новенькие: 
      %span= "(#{distance_of_time_in_words_to_now(TwitUser.new_listed.first.created_at)})"
    %ol  
      - TwitUser.new_listed.each do |user|
        %li
          = link_to image_tag(user.profile_image_url,:class=>'profile_image_small',:title=>user.name),"http://twitter.com/#{user.screen_name}"
          = link_to user.screen_name,"http://twitter.com/#{user.screen_name}",:title=>user.name
          = raw "(<i>#{user.name}</i>)"
  .clear
-#   .twitter_small
  -#   .image
  -#   = link_to image_tag(twitter.profile_image_url,:class=>'profile_image_small'),"http://twitter.com/#{twitter.screen_name}"
  -# .screen_name
  -#   = link_to twitter.screen_name,"http://twitter.com/#{twitter.screen_name}"
  -#   -# .info
  -#   -#   = "(#{twitter.friends_count})"
  -# .clear
