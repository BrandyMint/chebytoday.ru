- TwitUser.cheboksary.each do |user|
  = link_to image_tag(user.profile_image_url,:class=>'small_profile_image',:alt=>"#{user.screen_name} (#{user.name}), друзей #{user.friends_count}"),"http://twitter.com/#{user.screen_name}"
  -# != link_to("Полный список (#{TwitUser.cheboksary.count}) &rarr;".html_safe,twitters_path,:class=>'tool right')
//.clear